# PLAN.md

## Поэтапный план реализации Telegram-бота FindOrigin

### Этап 1: Настройка проекта и окружения

- [ ] Создать новый проект на Next.js.
- [ ] Настроить структуру папок: `pages/api/webhook` для обработки входящих запросов.
- [ ] Установить зависимости (если нужны): `node-fetch`, `telegram-bot-api` (опционально), `dotenv`.
- [ ] Подключить переменные окружения (`.env.local`):
  - `TELEGRAM_BOT_TOKEN` — токен бота от @BotFather.
  - `WEBHOOK_URL` — URL, предоставленный Vercel (например, `https://findorigin.vercel.app/api/webhook`).

---

### Этап 2: Настройка Webhook для Telegram

- [ ] Написать `pages/api/webhook.ts`:
  - Принимает POST-запросы от Telegram.
  - Проверяет наличие `update.message.text` и `update.message.chat.id`.
  - Возвращает `200 OK` сразу после получения.
- [ ] Реализовать установку webhook при запуске (можно через отдельный API route `/api/set-webhook` или вручную через curl).
- [ ] Проверить, что Telegram доставляет сообщения на сервер.

---

### Этап 3: Обработка входящего сообщения

- [ ] Извлечь текст из `update.message.text`.
- [ ] Определить тип ввода:
  - Если это ссылка на Telegram-пост (`t.me/...`), извлечь ID поста и получить его содержимое (см. Этап 4).
  - Иначе — использовать введённый текст напрямую.
- [ ] Отправить пользователю подтверждение: "Получено, ищу источник..."

---

### Этап 4: Извлечение текста из Telegram-поста (опционально)

> Только если ввод — ссылка `t.me/username/post_id`

- [ ] Разобрать ссылку на username и post_id.
- [ ] Использовать Telegram API метод `getMessages` (требует доступ к каналу или публичному посту).
- [ ] Получить текст поста.
- [ ] Обработать ошибки (приватный канал, неверная ссылка).

---

### Этап 5: Извлечение ключевых элементов из текста

- [ ] Реализовать функцию `extractKeyElements(text)`:
  - Извлекает:
    - Ключевые утверждения (с помощью NLP-подобной логики или AI).
    - Даты (регулярные выражения или библиотека `chrono-node`).
    - Числа, проценты, статистику.
    - Имена (персоны, организации — можно использовать простые эвристики или AI).
    - Внешние ссылки (если есть).
- [ ] Вернуть объект с этими данными.

---

### Этап 6: Поиск возможных источников

- [ ] Сформировать поисковые запросы на основе ключевых элементов.
- [ ] Выполнить поиск через:
  - Google Programmable Search Engine (Custom Search JSON API) — основной способ.
  - Альтернатива: SerpAPI, или Scraping (осторожно, может быть блокировка).
- [ ] Фильтровать результаты:
  - Приоритет: официальные сайты (.gov, .org, .ru, .com), СМИ, научные публикации.
  - Исключить явно нерелевантные домены.
- [ ] Ограничить до 5–10 кандидатов.

---

### Этап 7: Семантическое сравнение с помощью AI

- [ ] Использовать LLM (например, через OpenAI API / Groq / Ollama):
  - Для каждого найденного источника:
    - Извлечь основной текст (через `readability`, `fetch + parsing`).
    - Сравнить смысл с исходным утверждением.
  - Задача: оценить, совпадает ли смысл (не точный текст).
- [ ] Получить:
  - Краткое обоснование соответствия.
  - Оценку уверенности (например, "Высокая", "Средняя", "Низкая").

---

### Этап 8: Формирование и отправка ответа

- [ ] Отсортировать результаты по уверенности (топ-3).
- [ ] Сформировать сообщение:
🌐 Название сайта Оценка: Высокая Обоснование: Текст содержит аналогичное утверждение о ...

...

- [ ] Отправить в чат через `sendMessage` Telegram API.

---

### Этап 9: Обработка ошибок и крайних случаев

- [ ] Обработать:
- Пустой ввод.
- Недоступность внешних API.
- Ошибки парсинга.
- Приватные/недоступные Telegram-посты.
- [ ] Отправлять понятные сообщения об ошибках.

---

### Этап 10: Деплой на Vercel

- [ ] Подключить репозиторий к Vercel.
- [ ] Убедиться, что сборка проходит.
- [ ] Установить переменные окружения в интерфейсе Vercel.
- [ ] Вызвать `/api/set-webhook` после деплоя.
- [ ] Протестировать бота в Telegram.

---

### Этап 11: Тестирование и доработка

- [ ] Протестировать:
- Простой текст.
- Ссылки на публичные посты.
- Граничные случаи (слишком короткий текст, спам и т.д.).
- [ ] Оптимизировать время ответа (кеширование, асинхронность).
- [ ] Добавить логирование (console.log или через сервис вроде Logtail).

---

### Этап 12: Документация и запуск

- [ ] Написать краткое руководство для пользователя (в README).
- [ ] Запустить бота в продакшен.
- [ ] Сделать публикацию (например, в Telegram-канале, Хабр, Reddit).

---

✅ **Готово, когда**:  
Пользователь присылает текст или ссылку — бот в течение 10–20 секунд возвращает 1–3 возможных источника с оценкой уверенности.
